[package]
name = "gt-site"
version = "0.1.3"
edition = "2021"
description = "GitTributary static HTML site builder"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
walkdir = "2"

[dev-dependencies]
tempfile = "3"
