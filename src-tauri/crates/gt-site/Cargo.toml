[package]
name = "gt-site"
version = "0.1.1"
edition = "2021"
description = "GitTributary static HTML site builder"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
