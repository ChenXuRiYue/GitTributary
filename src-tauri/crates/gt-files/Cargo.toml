[package]
name = "gt-files"
version = "0.1.0"
edition = "2021"
description = "Root-confined filesystem primitives for GitTributary"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
